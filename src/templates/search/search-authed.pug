extends ../layout/main.pug

block content
  h2 Search project (if you are seeing this you are authed)
  div.search-card-box
    each item in values
        div.search-card
          p=item.name
          p=item.short_description